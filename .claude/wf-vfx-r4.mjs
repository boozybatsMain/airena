export const meta = {
  name: 'vfx-polish-r4',
  description: 'Round 4: close the acceptance wave-2 items and the six checkdecay findings',
  phases: [
    { title: 'Круг 4', detail: 'по агенту на модуль: приёмка 74/100 назвала конкретное' },
  ],
};

const BRIEF = '/private/tmp/claude-501/-Users-boozybats-Public-Repos-work-Airena/481cc3fa-4508-484d-be0d-a7ab7ea08a68/scratchpad/brief.md';
const VERDICTS = '/private/tmp/claude-501/-Users-boozybats-Public-Repos-work-Airena/481cc3fa-4508-484d-be0d-a7ab7ea08a68/scratchpad/acceptance-r2.json';
const PORT = 56700;

const TARGETS = [
  {
    el: 'kinetic', file: 'src/viewer/vfx/kinetic.js', kinds: 'jump,cone,bolt,beam,zone',
    task: `ЧЕТЫРЕ ПУНКТА, ВСЕ ИЗ ВОЛНЫ ПРИЁМКИ 74/100.
1. ПРЫЖОК РИСУЕТ НОЛЬ. Судья контактных: «kinetic/jump — пустая арена, строки 4
   и 5 листа j2-jump-top.png пусты на всех трёх моментах, 0.03 % по vfxclean»,
   потому что модуль не экспортирует «jump», а штатный hop почти невидим.
   Либо свой прыжок, либо честный вывод с замером, что штатного хватает.
2. КОНУС НЕ ЧИТАЕТСЯ С 26 м: пик 0.88 % против 12.51 % у молнии и 2.89 % у
   жара; kinetic-cone-t0_15-top.png — серая россыпь щепок без клина.
3. БОЛТ НЕВИДИМ: 5×8 пикселей сверху, видна только тень
   (f2-forms/kinetic-bolt-t0_15-top.png). Нужен хвост или контраст.
4. МЕТКА ЛУЧА У КАСТЕРА — «ЛИШАЙНИК»: тёмный бублик с зубчатой кромкой
   205×225 px (f2-forms/kinetic-beam-t0_15-top.png), тот же дефект, который
   уже убрали у кислоты.
ПАЛИТРА: медиана #284F6C темнее самого тёмного цвета собственной палитры
#5d7183 (registry.js:368) — вернуть в объявленный серо-стальной диапазон.
СТАТУС НЕ ТРОГАЙ: ведущий только что заменил там выключатель на огибающую
(«left/kit.FADE_MIN», полином 3t²−2t³) — гейт по кинетике зелёный.`,
  },
  {
    el: 'void', file: 'src/viewer/vfx/void.js', kinds: 'jump,dash,bolt',
    task: `ТРИ ПУНКТА ИЗ ВОЛНЫ ПРИЁМКИ.
1. ПРЫЖОК РИСУЕТ НОЛЬ (0.07 % по vfxclean, строка листа пуста на всех трёх
   моментах): модуль не экспортирует «jump».
2. РЫВОК СТАТИЧЕН: void-dash-t0_70-top.png — сплошной непрозрачный клинок без
   структуры, 1.07 → 1.05 → 0.99 % за 0.7 с, то есть в окне съёмки не меняется
   вообще. Эталон рядом: arc/dash даёт 0.83 → 3.01 → 0.14.
3. БОЛТ: однопиксельный ступенчатый край и плоская заливка индиго
   (f2-forms/void-bolt-t1_50-top.png, 95×92 px). Нужен мягкий спад по краю и
   уход В ЧЁРНОЕ, а не в синий — это дыра, а не краска.`,
  },
  {
    el: 'frost', file: 'src/viewer/vfx/ice.js', kinds: 'beam,zone,wall,self',
    task: `ТРИ ПУНКТА ИЗ ВОЛНЫ ПРИЁМКИ.
1. ЛУЧ — ВИДИМЫЙ КВАД: ровный срез открытого цилиндра, нужен спад альфы к
   торцу и сужение растущего кончика (ice.js около 1334;
   f2-forms/frost-beam-t0_15-top.png и -low.png).
2. ЗОНА — НЕТЕКСТУРИРОВАННЫЙ ЦИЛИНДР, А ТУМАН ЛОЖИТСЯ НА HUD:
   frost-zone-t1_50-low.png — жёсткая эллиптическая крышка и прямые стенки;
   f2-fight-frost-arc/fight-03-t6_0.png — белые круглые кляксы тумана стоят
   ПОВЕРХ шапки боевого HUD. Нужен мягкий край и чтобы взвесь не лезла в кадр
   камеры вещания поверх интерфейса.
3. СТЕНА БЛЕДНЕЕ ЯЩИКОВ-ПРЕПЯТСТВИЙ, хотя атом кладёт настоящий солид в
   world.obstacles на 5 с (frost-wall-t2_50-broadcast.png; эталон рядом —
   kinetic-wall-t2_50-broadcast.png). Сверху это вообще клякса.`,
  },
  {
    el: 'radiation', file: 'src/viewer/vfx/radiation.js', kinds: 'zone,cone,lob',
    task: `ДВА ПУНКТА ПРИЁМКИ + ДВЕ СТРОКИ ГЕЙТА.
1. ЗОНА ЧИТАЕТСЯ МУСОРОМ: radiation-zone-t1_50-broadcast.png — одинаковые
   плоские жёлтые КВАДРАТЫ по полу, без свечения и без опасного тона. Спрайты
   перестать показывать квадратами (форма, поворот, мягкая альфа), вернуть
   радиации собственный свет.
2. ЛУЖА НАВЕСА — россыпь твёрдых жёлтых квадратов в комковатой кромке; три
   таких пятна дают 2.43 % арены сразу (f2-fight-radiation-acid/
   fight-02-t4_3.png).
3. ГЕЙТ «checkdecay» КРАСНЫЙ НА ТЕБЕ, две строки:
   «radiation/zone: общая огибающая падает за 0.15 c (нужно ≥ 0.18)» и
   «radiation/cone: … 0.15 c». Это стоячие носители: они обязаны гаснуть, а не
   сниматься. Рецепт есть в «void.js:1902» — доля left/kit.FADE_MIN, полином
   3t²−2t³. Проверять: node tools/checkdecay.mjs.`,
  },
  {
    el: 'arc', file: 'src/viewer/vfx/arc/zone.js', kinds: 'zone,self',
    task: `ДВЕ ВЕЩИ.
1. ГЕЙТ КРАСНЫЙ: «arc/zone: общая огибающая падает за 0.15 c (нужно ≥ 0.18)».
   Стоячий носитель обязан гаснуть огибающей, а не сниматься с конца жизни.
   Рецепт — «void.js:1902»: left/kit.FADE_MIN, полином 3t²−2t³.
2. ОБОЛОЧКА: судья зон — «arc/self на t=1.50 занимает 0.005–0.26 % арены, на
   листе j2-self-low.png клетка пуста с 1.50 до 4.00, при том что щит по
   реестру живёт 5 с (registry.js:332)». Проверь, живёт ли оболочка молнии
   свои пять секунд ВИЗУАЛЬНО, и если нет — почему: собственный носитель
   слишком короток или его вообще нет. Файл оболочки ищи сам в src/viewer/vfx/arc/.`,
  },
  {
    el: 'gravity', file: 'src/viewer/vfx/gravity.js', kinds: 'zone,self',
    task: `ДВЕ ВЕЩИ.
1. ЯДРО ЗОНЫ — ОТЛАДОЧНЫЙ ШАР: gravity-zone-t0_15-low.png и
   gravity-zone-t1_50-broadcast.png показывают матовую сферу без обода, без
   шейдинга и без искажения. Кольца по полу судья ХВАЛИТ («сделаны хорошо и
   уже читаются гравитацией») — их не ломать; отстаёт именно ядро.
2. ГЕЙТ КРАСНЫЙ: «gravity/zone: общая огибающая падает за 0.08 c (нужно ≥
   0.18)» — стоячий носитель снимается вместо затухания. Рецепт —
   «void.js:1902». Проверять: node tools/checkdecay.mjs.`,
  },
  {
    el: 'ember', file: 'src/viewer/vfx/fire.js', kinds: 'zone',
    task: `ОДНА СТРОКА ГЕЙТА, НО СМОТРИ ШИРЕ.
«ember/zone: общая огибающая падает за 0.08 c (нужно ≥ 0.18)» — носитель зоны
жара живёт 4.22 с и снимается со сцены при ненулевой плотности вместо того,
чтобы гаснуть. Рецепт — «void.js:1902»: доля left/kit.FADE_MIN, сглаженная
полиномом 3t²−2t³.
ВТОРОЕ, СВОИМИ ГЛАЗАМИ: по vfxclean зона жара — вторая по занятости арены из
всех шестидесяти форм (2.23 % на 4.00 с, дым). Это в пределах, но это
предпоследнее место по заказу «эффект произошёл — и его нет». Посмотри кадры
на 2.6 и 4.0 и реши, дым ли это или уже захламление; если второе — прибери,
не теряя красоты пожара.`,
  },
  {
    el: 'laser', file: 'src/viewer/vfx/laser.js', kinds: 'beam',
    task: `ОДНА СТРОКА ГЕЙТА + ОДНА СТРОКА ПРИЁМКИ.
1. «laser/beam: общая огибающая падает за 0.15 c (нужно ≥ 0.18)» — стоячий
   носитель луча снимается вместо затухания. Рецепт — «void.js:1902».
2. Судья дальних форм: «ни один из 26 кадров f2-fight-laser-void не застаёт
   луч в касте». Сними луч сам на 0.15/0.4/1.5 и убедись глазами, что он
   читается лазером, а не белой палкой.`,
  },
];

const SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['element', 'addressed', 'declined', 'measured', 'gate_green', 'frames'],
  properties: {
    element: { type: 'string' },
    addressed: {
      type: 'array',
      items: {
        type: 'object', additionalProperties: false,
        required: ['point', 'fix', 'evidence'],
        properties: { point: { type: 'string' }, fix: { type: 'string' }, evidence: { type: 'string' } },
      },
    },
    declined: { type: 'array', items: { type: 'string' } },
    measured: { type: 'string' },
    gate_green: { type: 'boolean' },
    frames: { type: 'string' },
  },
};

const shared = `ЧТО ДЕЛАЕТ ВЕДУЩИЙ ПАРАЛЛЕЛЬНО — не трогай, будет столкновение правок:
- «src/viewer/vfx.js»: подписи атомов удара (размер восьми частичных подписей,
  направление у boost/weaken, развод stun и blind), жест эффекта, штатная зона;
- «src/viewer/vfx/kit.js»: пороги слоя;
- «tools/vfxsheet.mjs», «tools/checkdecay.mjs», пересъёмка боёв.
Нужна правка в общем слое — пиши в «declined», ведущий сделает.

ЧТО ИЗМЕНИЛОСЬ В ГЕЙТЕ (важно, если он у тебя красный):
«checkdecay» теперь меряет уход ЭФФЕКТА ЦЕЛИКОМ (максимум по всем носителям
формы), на ЧИСТОМ прогоне без разогрева, и только у СТОЯЧИХ форм — зона,
оболочка, статус, конус, луч, стена. Бегущие (болт, навес, прыжок, рывок,
мигание, замах, удар) кончаются прилётом, с них проба снята. Порог: огибающая
обязана падать с половины пика до пяти процентов не быстрее 0.18 с.`;

phase('Круг 4')
const fixed = await parallel(TARGETS.map((t) => () => agent(
  `Ты доводишь модуль эффектов до приёмки. Репозиторий /Users/boozybats/Public/Repos/work/Airena.
Сначала прочитай ${BRIEF} — заказ основателя, судить надо по нему.

ТВОЙ МОДУЛЬ: ${t.file} — стихия «${t.el}».

ВОЛНА ПРИЁМКИ ПОСТАВИЛА СЛОЮ 74 ИЗ 100 при пороге 75. Полные вердикты восьми
судей — в ${VERDICTS} (поля «angle», «score», «verdict», «must_fix»,
«evidence»). Прочитай их и найди всё, что относится к твоей стихии.

ЧТО ИМЕННО ТЕБЕ:
${t.task}

${shared}

ПОРЯДОК
1. Открой кадры, на которые ссылаются судьи (Read на .png — ГЛАЗАМИ).
2. По каждому пункту: чиню или отказываюсь с доводом и замером. Отказ с
   доводом — нормальный результат; отказ без замера — нет.
3. Правь ТОЛЬКО свой модуль (и файлы своей стихии). Комментарии по-русски, в
   голосе файла: почему так, и с числом.
4. Кадры под замком GPU, канонический каталог — reports/vfx/r4-${t.el}:
   tools/vfxshot-lock.sh --port=${PORT} --el=${t.el} --kind=${t.kinds} \\
     --cams=broadcast,low,top --moments=0.15,0.4,1.5,2.6,4.0 --out=$PWD/reports/vfx/r4-${t.el}
   Посмотри снятое ГЛАЗАМИ и напиши в «frames», что видно.
5. node tools/checkdecay.mjs должен быть зелёным по твоей стихии; node --check
   на изменённые файлы. В «gate_green» — правда, а не намерение.

Верни отчёт по схеме: в «evidence» — числа и имена кадров.`,
  { label: `круг4:${t.el}`, phase: 'Круг 4', schema: SCHEMA },
)));

const rows = fixed.filter(Boolean);
log(`круг 4: отчитались ${rows.length} из ${TARGETS.length}`)
return { fixed: rows };
