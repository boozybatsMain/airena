export const meta = {
  name: 'vfx-polish-r5',
  description: 'Round 5: close what acceptance wave 3 named, per module',
  phases: [{ title: 'Круг 5', detail: 'по агенту на модуль: волна приёмки поставила 71' }],
};

const BRIEF = '/private/tmp/claude-501/-Users-boozybats-Public-Repos-work-Airena/481cc3fa-4508-484d-be0d-a7ab7ea08a68/scratchpad/brief.md';
const VERDICTS = '/private/tmp/claude-501/-Users-boozybats-Public-Repos-work-Airena/481cc3fa-4508-484d-be0d-a7ab7ea08a68/scratchpad/acceptance-r3.json';

const TARGETS = [
  {
    el: 'arc', file: 'src/viewer/vfx/arc/*.js', kinds: 'self,wall,beam,status,zone',
    task: `ПЯТЬ ПУНКТОВ, ВСЕ ИЗ ВОЛНЫ ПРИЁМКИ.
1. ГЕЙТ КРАСНЫЙ, ДВЕ СТРОКИ: «arc/wall: общая огибающая падает за 0.14 c» и
   «arc/beam: … 0.17 c» при пороге 0.18. Раньше гейт печатал по ним зелёное
   0.22–0.23 — он ошибался: шаг пробы был 0.075 с и округлял только в сторону
   зачёта. Ведущий уменьшил шаг до 0.006 с, и цифры стали настоящими.
   Пятисекундная стена стоит в полную плотность и пропадает за четверть
   секунды — это ровно «резко», против чего написан заказ.
2. СОР ВОКРУГ ЩИТА: на f3-forms/arc-self-t4_00-broadcast.png помимо оболочки
   по полу ползут два разряда метра на четыре и рассыпано около тридцати
   синих обломков. Оболочка — сообщение, обломки в четырёх метрах от тела —
   нет. arc/self держит 4.08 % арены на 4.00 с — БОЛЬШЕ ВСЕХ в наборе.
3. ГОРЕНИЕ У МОЛНИИ НЕ РИСУЕТСЯ НИЧЕМ: vfxclean по f3-statuses даёт
   arc/status.burn = 0.00 %, диф против пустого кадра — РОВНО НОЛЬ пикселей
   на обоих ракурсах. У жара 1 707, у мороза 8 225, у пустоты 12 907. Пара
   arc+burn законна по грамматике, значит следующее существо может её взять.
4. СТЕНА НЕ ЧИТАЕТСЯ СТЕНОЙ: f3-moves/arc-wall-t0_35-broadcast.png — почти
   прозрачная бледно-лиловая панель по колено, по ней ползают белые нити.
   Рядом frost/wall и kinetic/wall — плотные объёмы в рост бойца. Реестр
   обещает «вырастающую плиту» (registry.js:337), klass WORLD, 5 с.
5. КОНУС МОЛНИИ НЕ ПРОВЕРЕН НА УХОД НИЧЕМ: гейт печатает «носитель живёт
   0.80 c — короче 1.2 c», а кадров между 0.15 и 1.50 с нет. При этом это
   самый громкий конус набора: 4.68 % арены на пике.`,
  },
  {
    el: 'kinetic', file: 'src/viewer/vfx/kinetic.js', kinds: 'self,wall,dash,bolt,lob',
    task: `ЧЕТЫРЕ ПУНКТА.
1. ГЕЙТ КРАСНЫЙ: «kinetic/self: огибающая падает за 0.13 c» и «kinetic/wall:
   … 0.17 c» при пороге 0.18. Прежнее зелёное было ошибкой инструмента (шаг
   пробы округлял в сторону зачёта), теперь шаг 0.006 с.
2. РЫВОК НЕ НАХОДИТСЯ ГЛАЗАМИ С ТРАНСЛЯЦИИ: kinetic/dash даёт 0.15/0.18/0.16 %
   арены против 0.60–1.05 % у молнии и 0.56–1.66 % у жара. Кадр
   f3-moves/kinetic-dash-t0_35-broadcast.png — три бледно-серые плиты на белом
   полу. В бою то же: f3-fight-laser-kinetic/fight-08-t12_3.png, в index.json
   у кадра стоит fx «dash:kinetic», а рывка на кадре не видно.
3. СНАРЯД ВЫГЛЯДИТ НЕЗАКОНЧЕННЫМ: судья дальних форм — «плоский шестиугольник
   плюс брус постоянного сечения без сужения и без мягкого конца»
   (f3-forms/kinetic-lob-t0_15-low.png, kinetic-bolt-t0_15-low.png). Свет
   тебе запрещён домовым правилом файла — значит форма и силуэт.
4. ВСПЛЕСК kinetic/self «глотает своего же кастера» (судья настоящего боя
   прошлого круга) и даёт 0.039 % арены на 1.50 с при пятисекундном бафе.`,
  },
  {
    el: 'ember', file: 'src/viewer/vfx/fire.js', kinds: 'beam,cone,bolt',
    task: `ТРИ ПУНКТА, ОДИН ИЗ НИХ — В НАСТОЯЩЕМ БОЮ.
1. ГЕЙТ КРАСНЫЙ: «ember/beam: общая огибающая падает за 0.17 c» при пороге
   0.18. Прежнее зелёное 0.23 было ошибкой шага пробы, теперь шаг 0.006 с.
   Рецепт ухода огибающей — в laser.js (урок 14) и void.js.
2. КОНУС В БОЮ — БЕСФОРМЕННЫЙ БЕЛЁСЫЙ КОМ. Судья контактных форм:
   «reports/vfx/f3-fight-ember-arc/fight-06-t9_3.png (fx cone:ember): вместо
   струи пламени крупный непрозрачный кремово-белый ком, целиком проглотивший
   кастера, ни направления, ни оранжевого; столб искр уходит за верхнюю кромку
   кадра и рисуется ПОВЕРХ подписей HUD». Второй кадр того же конуса
   (fight-15-t24_1.png) — наоборот, почти ничего. Ни один не показывает того,
   что показывает стенд. Это одновременно «некрасиво» и «захламляет экран», и
   найдено там, где основатель просил смотреть.
3. ГОЛОВА БОЛТА ОБЕСЦВЕЧЕНА НА БЕЛОМ ПОЛУ: 240,239,236 против пола
   225,223,221 — снаряд огня читается только по шлейфу.`,
  },
  {
    el: 'gravity', file: 'src/viewer/vfx/gravity.js', kinds: 'status,zone,self',
    task: `ОДНА КРАСНАЯ СТРОКА ГЕЙТА И ОДНА ОБЯЗАННОСТЬ.
1. «gravity/status: общая огибающая падает за 0.14 c» при пороге 0.18. Раньше
   печаталось зелёное 0.22 — ошибка шага пробы, теперь шаг 0.006 с. Полный
   уход от полной плотности до нуля — 0.28 с.
2. ТВОЮ СТИХИЮ В ЭТОМ КРУГЕ НЕ СНИМАЛИ ВООБЩЕ. Съёмка по умолчанию брала
   пять стихий из десяти (дефект инструмента, ведущий его починил), и
   gravity.js правился ПОСЛЕ своей последней галереи. Сними свои формы сам и
   посмотри ГЛАЗАМИ — возможно, там есть что-то, чего никто не видел.`,
  },
  {
    el: 'frost', file: 'src/viewer/vfx/ice.js', kinds: 'zone,self',
    task: `ОДИН ПУНКТ, НО ЕГО НАШЛИ ДВА СУДЬИ НЕЗАВИСИМО.
ТУМАН ЗОНЫ ПОКАЗЫВАЕТ СВОЙ КВАД: обрезан прямоугольником с прямыми левой,
верхней и правой кромками — читается стеклянным ящиком, а не туманом. Видно
в настоящем бою (f3-fight-frost-void/fight-11-t15_7.png, примерно x 913–1128,
y 202–470, и следующий кадр fight-12-t16_2.png) и в галерее
(f3-forms/frost-zone-t1_50-low.png). Последний коммит репозитория как раз про
расширение квада под лужи — у объёмного тумана мороза он всё ещё режет форму.
ВТОРОЕ: frost/self держит 2.84 % арены на 4.00 с — второе место в наборе.
Купол живёт 6 с, это законно; но посмотри кадры на 4.0 и реши, всё ли там
сообщение, или часть — сор.`,
  },
  {
    el: 'void', file: 'src/viewer/vfx/void.js', kinds: 'self,cone,zone,status',
    task: `ДВА ПУНКТА ИЗ СУДЬИ РАЗЛИЧИМОСТИ (66 из 100 — худшая оценка круга).
1. ОБОЛОЧКА НЕ ЖИВЁТ СВОЮ ЖИЗНЬ: щит по реестру 5 с (registry.js:332), а
   void/self падает до 0.00 % арены уже к 2.60 с. У молнии и мороза оболочки
   стоят свою жизнь целиком. «Либо все стоят, либо реестр врёт».
2. ДВА СЛЕДА ПУСТОТЫ ГОВОРЯТ РАЗНОЕ: в f3-forms/void-cone-t0_15-low.png
   СКВОЗЬ след видна сетка пола — то есть краска; в void-zone след пол
   ПЕРЕКРЫВАЕТ — то есть дыра. Одна стихия обязана говорить одно слово.
   Плюс лужа void/status.burn (12 907 пикселей — самая заметная в наборе)
   неотличима от зоны-дыры.`,
  },
  {
    el: 'acid+radiation', file: 'src/viewer/vfx/acid.js и src/viewer/vfx/radiation.js', kinds: 'zone,cone,lob,bolt',
    task: `ВАС ПУТАЮТ ДРУГ С ДРУГОМ, И ЭТО ЗАМЕРЕНО ПО ТОНУ.
Судья различимости (66 из 100): «В f3-fight-acid-radiation/fight-11-t15_8.png
журнал говорит zone:acid, а на полу лежит метка с тоном 48.6° — то есть
радиационная (49.3°), тогда как кислота на стенде 84.0°». Две стихии, чьи
следы лежат на полу жёлто-зелёными пятнами, обязаны разойтись НЕ ЯРКОСТЬЮ, а
родом вещества: кислота — жидкость (течёт, собирается в лужу, съедает край),
радиация — сухая пыль и свет (не течёт, светится, сыплется).
ВТОРОЕ: ваших форм в этом круге не снимали вообще (дефект инструмента съёмки,
ведущий починил). Сними обе стихии сам, все формы, три ракурса, и посмотри
ГЛАЗАМИ — четыре из пяти пропущенных модулей правились как раз перед этим.
ТРЕТЬЕ: судья дальних форм просит снять acid/lob и acid/bolt В ПОЛЁТЕ —
«на обоих имеющихся кадрах снаряд уже у тела и формы не видно».`,
  },
];

const SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['element', 'addressed', 'declined', 'measured', 'gate_green', 'frames'],
  properties: {
    element: { type: 'string' },
    addressed: { type: 'array', items: { type: 'object', additionalProperties: false,
      required: ['point', 'fix', 'evidence'],
      properties: { point: { type: 'string' }, fix: { type: 'string' }, evidence: { type: 'string' } } } },
    declined: { type: 'array', items: { type: 'string' } },
    measured: { type: 'string' },
    gate_green: { type: 'boolean' },
    frames: { type: 'string' },
  },
};

const shared = `ЧТО ПОЧИНИЛ ВЕДУЩИЙ — не делай заново и не спорь с этим:
- ШАГ ПРОБЫ ГЕЙТА. Был 0.075 с при пороге 0.18 — округлял только в сторону
  зачёта. Судья не поверил зелёному, прогнал шагом 0.01 и нашёл шесть форм
  ниже порога. Теперь шаг 0.006 с, и эти шесть покраснели по делу.
- СПИСОК СТИХИЙ СЪЁМКИ. В «tools/vfxshot.mjs» стояло пять имён списком (те,
  что были до выпуска остальных пяти), и прогон молча снимал половину набора.
  Теперь список берётся из реестра.
- Жесты статуса разведены ТОПОЛОГИЕЙ и ВЫСОТОЙ, а не наклоном: оглушение —
  три разомкнутые дуги над головой, немота — замкнутое перечёркнутое кольцо
  у груди, ослепление — сплошная планка на высоте глаз. Наклон не переживает
  смену ракурса, топология переживает.
- Подписи удара и жесты рисуются ЧЕРНИЛАМИ ФУНКЦИИ (единый тёмный цвет), а не
  палитрой стихии: пол арены белый у всех, и палитра половины стихий на нём
  не читалась. Фактуру стихии несут частицы.
Нужна правка в общем слое — пиши в «declined».

СНИМАТЬ ТАК (замок GPU обязателен, вьювер подними свой):
  PORT=<свой порт> node src/server/index.js &
  tools/vfxshot-lock.sh --port=<свой порт> --el=<стихия> --kind=<формы> \\
    --cams=broadcast,low,top --moments=0.15,0.4,1.5,2.6,4.0,5.6 --out=$PWD/reports/vfx/r5-<стихия>
НЕ используй порт 8787 для стойки: там продуктовый сервер с чужим боем.`;

phase('Круг 5')
const fixed = await parallel(TARGETS.map((t) => () => agent(
  `Ты доводишь модуль эффектов до приёмки. Репозиторий /Users/boozybats/Public/Repos/work/Airena.
Сначала прочитай ${BRIEF} — заказ основателя, судить надо по нему.

ТВОЙ МОДУЛЬ: ${t.file} — стихия «${t.el}».

ВОЛНА ПРИЁМКИ ПОСТАВИЛА СЛОЮ 71 ИЗ 100 при пороге 75, и это НИЖЕ прошлого
круга (74). Полные вердикты восьми судей — в ${VERDICTS}. Прочитай их и найди
всё, что относится к твоей стихии; там больше подробностей, чем здесь.

ЧТО ИМЕННО ТЕБЕ:
${t.task}

${shared}

ПОРЯДОК
1. Открой кадры, на которые ссылаются судьи (Read на .png — ГЛАЗАМИ).
2. По каждому пункту: чиню или отказываюсь с доводом и замером. Отказ с
   замером — нормальный результат; отказ без замера — нет.
3. Правь ТОЛЬКО файлы своей стихии. Комментарии по-русски, в голосе файла:
   почему так, и с числом.
4. node tools/checkdecay.mjs должен быть зелёным ПО ТВОЕЙ СТИХИИ (по чужим
   может быть красным — их чинят параллельно). node --check на файлы.
5. Сними свои формы и посмотри ГЛАЗАМИ. В «frames» напиши, что видно.

Верни отчёт по схеме: в «evidence» — числа и имена кадров.`,
  { label: `круг5:${t.el}`, phase: 'Круг 5', schema: SCHEMA },
)));

const rows = fixed.filter(Boolean);
log(`круг 5: отчитались ${rows.length} из ${TARGETS.length}`)
return { fixed: rows };
