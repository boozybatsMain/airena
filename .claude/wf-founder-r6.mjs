export const meta = {
  name: 'founder-r6',
  description: 'Founder order: remove time, land-only lob with brain-controlled range, smarter spacing, close red fade rows',
  phases: [
    { title: 'Работа', detail: 'четыре независимых куска: стихия, механика, мозги, эффекты' },
    { title: 'Проверка', detail: 'настоящие бои и гейты на собранном дереве' },
  ],
};

const ROOT = '/Users/boozybats/Public/Repos/work/Airena';

const SHARED = `РЕПОЗИТОРИЙ ${ROOT}. Комментарии по-русски, в голосе файла: почему так,
и с числом или именем кадра. Правь только свой участок — остальные три идут
параллельно. Нужна правка на чужом участке — пиши в «declined».

ЧТО УЖЕ СДЕЛАЛ ВЕДУЩИЙ (не переделывай):
- Стена аккаунта на локальном стенде больше не требует переменной окружения:
  режим один на весь сервер и живёт в «src/server/mode.js» — дев-стенд теперь
  означает ОТСУТСТВИЕ «AIRENA_SECRET», а не выставленный «AIRENA_DEV=1».
- В запись болта и навеса добавлено поле «aim» («deliver.js») — куда снаряд
  летит на самом деле, в отличие от «range» (предел умения).

ПРОВЕРКА: «npm test» должен остаться зелёным по ТВОЕЙ части (по чужим может
быть красным — их чинят рядом). «node --check» на каждый изменённый файл.`;

const TARGETS = [
  {
    key: 'time',
    label: 'убрать время',
    task: `ОСНОВАТЕЛЬ: «я просил отключить время — чтобы времени вообще не было в
скиллах, чтобы никто не мог создать время».

Это откат МОЕЙ ошибки: 04.09 я снял флаг «unreleased» сразу с пяти стихий,
включая время, ради «чтобы существо всегда генерировалось с правильными
скиллами». Про время такого заказа не было, и основатель просил обратного —
не в первый раз.

ЧТО СДЕЛАТЬ
1. «src/skills/registry.js»: вернуть времени «unreleased: true». Механизм уже
   есть и описан там же: «releasedElements()» прячет стихию от модели и от
   клиента, а «validateKit» отвечает «element_unreleased». Проверь ОБА пути
   своим прогоном: (а) грамматика, которую видит модель, не содержит времени;
   (б) набор со временем не проходит проверку.
2. Живые существа со временем в базе «data/airena.db» (ХРОНОМЕТР, ХРОНОГРОМ,
   ЧАСОВЩИК-ГРОМОВЕРЖЕЦ и любые другие — найди запросом, не по памяти) надо
   вывести из игры так, как это делает продукт: state, а не удаление строки.
   Посмотри, как retire делается в «src/server/creatures.js», и сделай так же.
   В отчёте назови поимённо, кого и сколько.
3. Счёт выпущенных стихий 10 → 9 тянет за собой числа, которые я же и правил:
   счёт прочтений грамматики, порог в «selfTest» и «checkspec», замороженные
   строки «SPEC.md». Найди их (grep по числу 678 и по слову «прочтен») и
   приведи в соответствие — числа должны СЧИТАТЬСЯ, а не быть вписаны.
4. Модуль вьювера «src/viewer/vfx/time.js» НЕ удаляй и не ломай: стихия
   остаётся в таблице и на стенде, закрыта только выдача наружу. Ровно для
   этого «unreleased» и заведён.
5. Документация: «README.md», «SPEC.md», «docs/VFX.md» — там, где написано
   «десять стихий» про ВЫПУЩЕННЫЕ, теперь девять. Гейт «node tools/checkdocs.mjs»
   обязан остаться зелёным.

Верни в отчёте: чем проверил каждый из двух путей (а) и (б), кого вывел из
игры, какие числа пересчитались.`,
  },
  {
    key: 'lob',
    label: 'навес: попадает только туда, куда упал',
    task: `ОСНОВАТЕЛЬ, ДВА ПУНКТА ОДНОЙ МЕХАНИКИ:
«Лобная атака, которая летит, — она не должна попадать, если пролетает над
существом. То есть она попадает только туда, куда попала.»
«Существо должно понимать, что оно может контролировать дальность этой атаки…
конкретно у лобной атаки существо может контролировать рейндж, и должно это
делать, чтобы попадать.»

ДЕФЕКТ ПОДТВЕРЖДЁН ЧТЕНИЕМ КОДА («src/core/deliver.js», tickProjectiles):
навес («arc: true») проверяется ТЕМ ЖЕ тестом близости, что и болт —
«dist2(nx, nz, you.x, you.z) <= you.def.radius + 0.35» — то есть попадает по
дороге, пролетая НАД целью. А когда его жизнь кончается, он засчитывается
ПРОМАХОМ («reason: 'aim'») и не применяет ничего. То есть сейчас навес
попадает там, где не должен, и не попадает там, где должен. Это противоречит
и заказу, и собственному описанию доставки в реестре: «перелетает препятствия,
ПРИЗЕМЛЯЕТСЯ С ЗАДЕРЖКОЙ».

ЧТО СДЕЛАТЬ — механика (реши сам, где именно, но семантика такая):
1. Навес НЕ задевает никого в полёте. Ни цель, ни укрытия (последнее уже так).
2. Навес ПРИЗЕМЛЯЕТСЯ в точку и бьёт по всем, кто в радиусе поражения от неё.
   Радиус — новое число в «src/skills/registry.js» у доставки «lob»; назови
   его и обоснуй в комментарии (у зоны для сравнения radius 3.0, у тела
   радиус около 0.5–0.9). Слишком большой радиус превратит навес в зону,
   слишком малый — в лотерею.
3. Промах остаётся промахом: если в точке приземления никого — «reason: 'aim'»
   как сейчас, со всей статистикой.
4. Запись «world.fx» навеса обязана нести ТУ ЖЕ точку приземления, по которой
   считает сим, — вьювер рисует лужу именно там (поле «aim» уже есть).

ЧТО СДЕЛАТЬ — управление дальностью:
5. «api.use(name, a, b)» уже принимает два числа, но сегодня они управляют
   только миганием («src/core/sim.js», startSkill). Для доставки «lob» первый
   аргумент — ЗАПРОШЕННАЯ ДАЛЬНОСТЬ в метрах, зажатая в разумные пределы
   (снизу — чтобы не бросать себе под ноги, сверху — предел умения с учётом
   «channelMul(me, 'range', …)», как уже считает deliver.js).
6. Умолчание, когда мозг не назвал дальность: расстояние до врага, а не
   предел умения. Наивный «use('k2')» должен ЛОЖИТЬСЯ НА ВРАГА, а не улетать
   за арену. (Замер прошлого круга: лужа кислоты легла ЗА КРАЙ АРЕНЫ и висела
   там 3.5 с ровно потому, что бралась предельная дальность.)
7. Точка приземления обязана оставаться ВНУТРИ арены: судья приёмки нашёл
   радиационный навес, лежащий три кадра подряд по ту сторону стены. Зажми по
   границам поля (границы бери из «src/core/config.js», не из числа в коде).
8. Мозг должен УЗНАТЬ, что дальностью можно управлять. В перцепции
   («kitView» в «src/core/sim.js») у навеса обязаны быть видны и предел, и
   радиус поражения. Сам ТЕКСТ инструкции для модели правит другой агент —
   напиши ему точную формулировку в поле «declined», начав со слов «АГЕНТУ
   ПРОМПТА:», и он вставит её дословно.

ПРОВЕРКА, обязательная: инвариант в «tests/» (посмотри, как устроены соседние)
на оба свойства — «пролетел над врагом и не задел» и «упал на врага и попал».
Плюс настоящий бой: «node tools/duel.mjs <кто> <кто>» с существом, у которого
есть навес, и покажи в отчёте, что попадания появились, а сквозные пропали.`,
  },
  {
    key: 'brains',
    label: 'мозги: дистанция и адаптация',
    task: `ОСНОВАТЕЛЬ: «Почему все роботы такие тупые? Почему они подходят друг к
другу вплотную, не выжидают на расстоянии? Короче, нет у них никакого ума.
Может быть, это просто LLM так написал, но мне важно это понять.»

ЧТО Я УЖЕ ВЫЯСНИЛ, ЧТОБЫ ТЫ НЕ НАЧИНАЛ С НУЛЯ (перепроверь, я мог ошибиться):
- Поведение пишет модель. Движок ей НЕ мешает: в перцепции есть живые
  параметры своего и чужого кита («kitView»), видимость, лучи, скорость врага,
  телеграф каста. То есть «тупость» — это то, что написала модель.
- Пример из базы, мозг КУВАЛДЫ (kit: конус/конус/рывок): он СОЗНАТЕЛЬНО жмётся
  вплотную — «Close range sticking: pressure the enemy, stay right on top of
  them». Для конусного набора (дальность 3.4 м) это, вообще говоря, ПРАВИЛЬНО.
  Не всякое сближение — глупость, и не надо чинить то, что верно.
- А вот что действительно плохо: тот же мозг вписал дальность ЧИСЛОМ —
  «dist <= 4.616», «dist >= 3.501» — вместо чтения «p.self.kit.k2.range».
  Правило F10 обещает: «смена кита НИКОГДА не требует регенерации мозга», а
  захардкоженное число это обещание ломает: игрок меняет набор, и мозг держит
  дистанцию под старый.

ЧТО СДЕЛАТЬ («src/brain/prompt.js» — он твой целиком):
1. Потребовать от модели БРАТЬ ДАЛЬНОСТИ ИЗ ПЕРЦЕПЦИИ, а не вписывать
   числами. Скажи прямо, что кит меняется между боями без перегенерации мозга,
   и вписанное число завтра будет враньём.
2. Дать доктрину дистанции — коротко и проверяемо, без «будь умным»:
   у бойца есть РАБОЧАЯ ДИСТАНЦИЯ, и она выводится из его собственного набора
   (самая длинная полезная дальность против самой короткой); сближаться стоит
   тогда, когда этого требует твоя доставка, а не по умолчанию; пока умения на
   откате — держать дистанцию, а не стоять в упоре; телеграф врага — повод
   разорвать, а не подойти. Пиши это как ФАКТЫ И СЛЕДСТВИЯ, в голосе файла:
   он весь построен на «каждое число — факт».
3. Вставить дословно формулировку про управление дальностью навеса, которую
   пришлёт агент механики (он положит её в свой отчёт со слов «АГЕНТУ
   ПРОМПТА:»). Если его формулировки нет — опиши сам по факту кода
   «src/core/sim.js» (startSkill) и напиши в отчёте, что взял из кода.
4. Проверить, что промпт не разъехался с движком: в файле есть гейт
   «node tools/checkprompt.mjs» или похожий — найди, чем промпт проверяется, и
   прогони. «node tools/checkdocs.mjs» тоже.

ВАЖНО ПРО ЗАМЕР: §1 репозитория стоит на том, что шесть эталонных мозгов
написаны ОДНИМ промптом. Прочитай в «brainPrompt» комментарий про это и не
сломай измерение: если твоя правка меняет форму промпта, скажи об этом в
отчёте прямо и назови, какие числа §1 после этого надо перемерить.

Верни в отчёте: что именно добавлено, и КАК ты убедился, что это улучшает
поведение, а не просто звучит хорошо.`,
  },
  {
    key: 'vfx',
    label: 'одиннадцать красных строк затухания',
    task: `ГЕЙТ «node tools/checkdecay.mjs» КРАСНЫЙ, 11 строк. Это не регресс: судья
приёмки показал, что прежний порог не рос вместе с формой — шестисекундная
оболочка законно гасла за пятую долю секунды, — и я поднял требование. Теперь
порог растёт как «0.045 · жизнь носителя», не ниже прежних 0.18 с и не выше
0.30 с (участок «половина пика → пять процентов», не длительность ухода).

СТРОКИ (форма: сколько сейчас / сколько нужно / жизнь носителя):
  kinetic/zone   0.19 / 0.19 / 4.20      kinetic/self   0.20 / 0.28 / 6.30
  kinetic/wall   0.21 / 0.22 / 5.00      kinetic/status 0.22 / 0.30 / 20.0
  frost/status   0.22 / 0.30 / 20.0      arc/self       0.23 / 0.29 / 6.45
  arc/status     0.22 / 0.30 / 20.0      void/status    0.22 / 0.30 / 20.0
  gravity/status 0.23 / 0.30 / 20.0      time/wall      0.21 / 0.25 / 5.55
  time/status    0.22 / 0.30 / 20.0

ШЕСТЬ ИЗ ОДИННАДЦАТИ — НОСИТЕЛИ СТАТУСА, и у всех ровно 0.22. Это не
совпадение: они все уходят по одному рецепту «доля left/kit.FADE_MIN,
сглаженная полиномом 3t²−2t³», а у полинома участок «половина → пять
процентов» равен 0.365 его длины: 0.365 × 0.6 = 0.219. То есть рецепт общий, и
чинить его надо ОДИН раз, а не шесть — найди, где он повторён, и реши, выносить
ли его в «kit.js» общей функцией. Это чистка дублирования, а не подгонка: длину
ухода статуса придётся поднять примерно до 0.82 с, чтобы участок стал 0.30.

ОСТАЛЬНЫЕ ПЯТЬ — свои: оболочки кинетики и молнии, зона и стена кинетики,
стена времени.

ГРАНИЦА: НЕ ТРОГАЙ порог в «tools/checkdecay.mjs» — он только что поставлен по
разбору судьи, и двигать проверяемое, чтобы пройти проверку, здесь запрещено.
Если считаешь порог неверным — пиши довод и замер в «declined».
Правь модули «src/viewer/vfx/**» и при необходимости «kit.js».
Судья прошлой волны также заметил, что оболочки мороза и молнии стоят в полную
силу до самого конца и потом падают: посмотри своими кадрами, так ли это, и
если так — уход должен начинаться раньше, а не быть длиннее на том же месте.`,
  },
];

const SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['part', 'done', 'declined', 'measured', 'green'],
  properties: {
    part: { type: 'string' },
    done: { type: 'array', items: { type: 'object', additionalProperties: false,
      required: ['point', 'fix', 'evidence'],
      properties: { point: { type: 'string' }, fix: { type: 'string' }, evidence: { type: 'string' } } } },
    declined: { type: 'array', items: { type: 'string' } },
    measured: { type: 'string' },
    green: { type: 'boolean' },
  },
};

phase('Работа')
const done = await parallel(TARGETS.map((t) => () => agent(
  `Ты закрываешь один пункт заказа основателя. ${SHARED}

ТВОЙ УЧАСТОК: ${t.label}.

${t.task}

Верни отчёт по схеме. В «evidence» — команды, числа и имена файлов, по которым
любой может перепроверить. В «green» — правда о гейтах по твоей части, а не
намерение.`,
  { label: `r6:${t.key}`, phase: 'Работа', schema: SCHEMA },
)));

const rows = done.filter(Boolean);
log(`сделано частей: ${rows.length} из ${TARGETS.length}`)
return { done: rows };
